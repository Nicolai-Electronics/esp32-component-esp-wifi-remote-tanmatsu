description: (Tanmatsu fork) Utility wrapper for esp_wifi functionality on remote targets
dependencies:
  idf:
    version: ">=5.3"
  nicolaielectronics/esp-hosted-tanmatsu:
    version: ">=1.0.0"
    rules:
      - if: "target in [esp32h2, esp32p4]"
